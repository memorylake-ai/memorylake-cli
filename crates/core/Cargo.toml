[package]
name = "memorylake-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Core library for the MemoryLake CLI"
repository.workspace = true

[dependencies]
thiserror = { workspace = true }
tracing = { workspace = true }
